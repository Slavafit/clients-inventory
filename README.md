# clients-inventory
parcel inventory bot
add form PC

текущая структура (Telegram-центричная):
.
├── index.js             <-- Точка входа Telegram
├── models/
│   ├── User.js          <-- Модели (используются обеими платформами)
│   ├── Product.js
│   └── ...
├── handlers/
│   ├── category.js      <-- Логика кнопок/меню Telegram
│   └── auth.js
└── ...

структура с WhatsApp

├── index.js             <-- 🟢 Запускает Telegraf (Telegram)
├── whatsapp.js          <-- 🟢 Запускает Express (WhatsApp Webhooks)
├── models/
│   └── User.js          <-- Общая модель. Добавить поле whatsappId.
├── handlers/
│   ├── category.js      <-- 🔴 Логика UI для Telegram (ctx.reply, ctx.editMessageText)
│   ├── whatsappUI.js    <-- 🟢 Логика UI для WhatsApp (sendListMessage, sendTextMessage)
│   └── auth.js
├── whatsappClient.js    <-- 🟢 Файл с функцией sendMessage к Meta API
└── ...

📑 Техническое Задание: Система автоматизации описей (Telegram & WhatsApp)
1. Общее описание системы
Система представляет собой двухплатформенного бота (Telegram и WhatsApp) для сбора описей товаров от клиентов, сохранения их в базу данных MongoDB и автоматической выгрузки в Google Sheets. Управление заказами осуществляется через единую админ-панель.

2. Технологический стек
Backend: Node.js (Express)

База данных: MongoDB Atlas (Mongoose)

Платформы: Telegram (Telegraf), WhatsApp (Meta Cloud API)

Интеграции: Google Sheets API v4

Туннелирование: ngrok (для локальной разработки)

3. Функциональные возможности клиента (User)
📥 Оформление заказа (Описи)
Процесс идентичен для обеих платформ (с учетом интерфейсных различий):

Авторизация: При первом входе бот запрашивает номер телефона и сохраняет telegramId или whatsappId.

Выбор категории: Отображение списка категорий из БД.

Выбор товара: Выбор конкретного товара внутри категории.

Ввод данных:

Ввод количества (проверка на число).

Ввод суммы (проверка на число).

Управление черновиком:

"Добавить еще": Возврат к выбору категорий.

"Завершить": Предварительный просмотр всей описи и расчет итоговой суммы.

Финальная отправка: После подтверждения данные отправляются в Google Sheets, а статус заказа в БД меняется на enviado.

🔔 Уведомления
Клиент получает мгновенные сообщения об изменении статуса его заказа (на ту платформу, с которой был сделан заказ).

Получение трек-номера сразу после его добавления администратором.

4. Функциональные возможности администратора (Admin)
Управление осуществляется преимущественно через Telegram-интерфейс, но логика едина.

🔑 Управление ролями
Назначение админа: Команда /makeadmin [ID] (доступно только владельцу через .env).

Смена ролей: Возможность переключать права пользователей через adminService.

📦 Управление заказами
Поиск по номеру телефона: Админ вводит номер клиента — бот выдает последний активный заказ.

Смена статусов:

nuevo (Новый)

en tramito (В обработке)

enviado (Отправлен)

entregado (Доставлен)

cancelado (Отменен)

Добавление трек-номера: При вводе трек-номера статус заказа автоматически меняется на enviado, и клиент получает уведомление.

5. Архитектура данных (Database Schema)
👤 Модель User
telegramId / whatsappId: Уникальные идентификаторы (поле TG не обязательно для пользователей WA и наоборот).

role: user или admin.

currentStep: Текущий шаг пользователя в воронке (FSM - Finite State Machine).

currentOrder: Массив объектов (товары, количество, цены) для временного хранения до оформления.

📝 Модель Order
userId: Ссылка на пользователя.

clientPhone: Номер телефона для быстрой связи и поиска.

items: Список заказанных позиций.

totalSum: Итоговая стоимость.

status: Текущее состояние.

trackingNumber: Номер для отслеживания посылки.

6. Взаимодействие компонентов (Логика)
Система построена по принципу Shared Service (Общий сервис):

Унификация: Файл adminService.js содержит всю бизнес-логику. Если завтра вы добавите третью платформу (например, Viber), вам не придется переписывать логику смены статусов.

Кросс-платформенность: Если админ нажал кнопку в Telegram, сервис проверяет, откуда пришел клиент. Если клиент из WhatsApp — уведомление уйдет через Meta API.

Надежность: Дублирование данных в MongoDB и Google Sheets гарантирует сохранность информации даже при сбоях API.

7. Планируемые расширения (Backlog)
Рассылки: Возможность админу отправить сообщение всем пользователям базы.

Аналитика: Генерация Excel-отчетов за месяц прямо в чат админу.

Редактирование цен: Возможность админу менять цены товаров через бота.